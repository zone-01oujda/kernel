[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]

[[bin]]
name = "kernel"
test = false
bench = false
crate-type = ["staticlib"]

[profile.release]
panic = "abort"
lto = true
code-model = "kernel"
